import receiptQrUrl from "assets/images/receipt-website-qr.png";

/** Bundled QR image (links to NGO website). Replace src/assets/images/receipt-website-qr.png to update. */
export const RECEIPT_WEBSITE_QR_SRC = receiptQrUrl;

export function getReceiptWebsiteQrSrc() {
  return RECEIPT_WEBSITE_QR_SRC;
}
